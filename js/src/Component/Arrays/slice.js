import React from "react";

const Slice = () => {
  const animals = ["ant", "bison", "camel", "duck", "elephant"];

  const newarray = animals.slice(3);
  console.log(newarray);
  // return (
  // );
};

export default Slice;
