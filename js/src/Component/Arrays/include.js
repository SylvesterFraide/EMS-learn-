export const Include = () => {

    // returns true if an array contains a certain value if the value is not found returns false

    const numbers = [1, 2, 3, 5, 5];
    const Includes4 = numbers.includes(4);

    console.log(Includes4);

    // return (
    //     <div>
    //         <h2>Array.prototype.includes()</h2>
    //         <p>The includes() method determines whether an array includes a certain value among its entries, returning true or false as appropriate.</p>
    //     </div>
    // );
}