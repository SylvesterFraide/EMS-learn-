export const ArrayLength = () => {
    //   Typically, the length property of an array returns the number of elements

      const animals = ["ant", "bison", "camel", "duck", "elephant"];

      console.log(animals.length);
//   return (<div>{animals.length}</div>);
};
